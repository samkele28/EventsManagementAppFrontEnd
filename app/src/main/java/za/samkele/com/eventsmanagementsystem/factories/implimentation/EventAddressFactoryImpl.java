package za.samkele.com.eventsmanagementsystem.factories.implimentation;

import za.samkele.com.eventsmanagementsystem.domain.EventAddress;
import za.samkele.com.eventsmanagementsystem.factories.EventAddressFactory;

/**
 * Created by Samkele on 4/22/2016.
 */
/*public class EventAddressFactoryImpl implements EventAddressFactory {
    private static EventAddressFactoryImpl eventAddressFactory = null;

    private EventAddressFactoryImpl(){

    }

    public static EventAddressFactoryImpl getInstance(){
        if(eventAddressFactory == null)
            eventAddressFactory = new EventAddressFactoryImpl();
        return eventAddressFactory;
    }

    public EventAddress getAddress(String streetAddress, String surburb, String postalCode) {
        EventAddress eventAddress = new EventAddress
                .Builder()
                .streetAddress(streetAddress)
                .surburb(surburb)
                .postalCode(postalCode)
                .build();
        return eventAddress;
    }
}*/
